apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: cache-operator-mutation-webhook
webhooks:
  - name: caches.kuroboros.io-mutation-webhook
    clientConfig:
      service:
        name: cache-operator-webhook
        namespace: cache-operator
        path: /v1beta1/cache/mutate
      caBundle: ""
    rules:
      - apiGroups:
          - kuroboros.io
        apiVersions:
          - v1beta1
        operations:
          - CREATE
        resources:
          - caches
        scope: Namespaced
    admissionReviewVersions: [ "v1" ]
    sideEffects: None
    failurePolicy: Fail